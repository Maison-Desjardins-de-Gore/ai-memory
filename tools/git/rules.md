Your project goes into github organisation Maison-Desjardins-de-Gore
Create repo with gh tool
When creating a repo, create a good .gitignore file
Never commit .env file, it should be in .gitignore
# Authentication
If GITHUB_TOKEN is not in your environment, you may need to provide it manually or use a credential helper. For persistent access, configure git to use SSH or a credential manager so you don't rely on environment variables in every session.
