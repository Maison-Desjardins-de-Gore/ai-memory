Project structure and rules

projects/
    PROJECT_NAME/

Most content should go in document within the project folder itself.

Content: where is the project, notes before the project repo is created

When to write:
- Before project's repose is created
- When the project is started, where to find the files (folder, repo name, etc)

This projects folder only contains memory notes, not the actual project.

The projects are mostly in this volume:
    /opt/ai-projects:/home/node/ai-projects
    You can access projects in your docker container, but also in the parent wsl.

The prod version, main branch, of the projects are in this volume
    /opt/ai-prod:/home/node/ai-prod

openclaw-core project is in the parent wsl here:
    /home/andy/openclaw